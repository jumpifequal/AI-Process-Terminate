; AIProcess-Terminate — target process list
;
; Add or remove keywords below (comma-separated, case-insensitive).
; A process is matched when its executable name contains any keyword as a
; substring, e.g. "claude" matches claude.exe, claude-helper.exe, etc.
;
; The application looks for this file next to AIProcess-Terminate.exe.
; If the file is missing or [Targets] / keywords is absent, the built-in
; default list is used instead.

[Targets]
keywords = claude, copilot, perplexity, codex, gemini, manus, antigravity, ChatGPT
